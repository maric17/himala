name: Build and Deploy Frontend

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/himala-nextjs:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: SSH Deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            cd ~/himala-everyday
            echo "${{ secrets.FRONTEND_SECRETS }}" > .env
            chmod 600 .env
            
            # Pull the latest image we just pushed to Docker Hub
            docker compose pull
            
            # Re-create the web container with the new image
            docker compose up -d --remove-orphans
            
            # CRITICAL: Refresh Nginx to clear any 502/Internal IP caching
            docker compose restart nginx
            
            # Clean up old, unused images to save disk space
            docker image prune -f